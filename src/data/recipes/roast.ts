import { RecipeSlug } from "@/enums";
import { Recipe } from ".";

const recipe: Recipe = {
	slug: RecipeSlug.Roast,
	title: "Roast",
	ingredients: [],
	steps: [],
	emojis: `🍗 🥔 🥕`,
};

export default recipe;
