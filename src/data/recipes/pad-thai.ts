import { RecipeSlug } from "@/enums";
import { Recipe } from ".";

const recipe: Recipe = {
	slug: RecipeSlug.PadThai,
	title: "Pad thai",
	ingredients: [],
	steps: [],
	emojis: `🍜 🥜 🍋`,
};

export default recipe;
