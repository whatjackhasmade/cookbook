"use client";

import styled from "styled-components";

export const Item = styled.article`
  position: relative;

  a {
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
  }
`;
